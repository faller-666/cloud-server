<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.cloud.files.dir.mapper.FileNodeMapper">

    <!-- 面包屑：自当前节点向上递归到根（R-C01）。PG 递归 CTE，一条 SQL 出结果 -->
    <select id="selectBreadcrumb" resultType="com.company.cloud.files.dir.dto.BreadcrumbItem">
        WITH RECURSIVE up AS (
            SELECT id, parent_id, name, 1 AS lvl
            FROM files
            WHERE id = #{id}
              AND owner_id = #{userId}
              AND deleted_at IS NULL
            UNION ALL
            SELECT f.id, f.parent_id, f.name, up.lvl + 1
            FROM files f
            JOIN up ON f.id = up.parent_id
            WHERE up.lvl &lt; 64
        )
        SELECT id, name FROM up ORDER BY lvl DESC
    </select>

    <!-- 级联软删：自身 + 全部子孙一次性打标（R-C04）。递归向下收集，单条 UPDATE -->
    <update id="softDeleteCascade">
        WITH RECURSIVE sub AS (
            SELECT id FROM files
            WHERE id = #{id}
              AND owner_id = #{userId}
              AND deleted_at IS NULL
            UNION ALL
            SELECT f.id FROM files f
            JOIN sub s ON f.parent_id = s.id
            WHERE f.deleted_at IS NULL
        )
        UPDATE files
        SET deleted_at = now(), updated_at = now()
        WHERE id IN (SELECT id FROM sub)
    </update>

    <!-- 移动校验：targetId 是否落在 id 的子树内（含自身）。> 0 即拒绝（R-C03） -->
    <select id="countInSubtree" resultType="int">
        WITH RECURSIVE sub AS (
            SELECT id FROM files WHERE id = #{id}
            UNION ALL
            SELECT f.id FROM files f
            JOIN sub s ON f.parent_id = s.id
        )
        SELECT COUNT(*) FROM sub WHERE id = #{targetId}
    </select>

    <!-- 回收站列表（R-C05）：本人全部已删除节点（含子孙），parentId 保留删除前原值，
         前端据此重建完整目录树；按删除时间倒序 -->
    <select id="selectRecyclePage" resultType="com.company.cloud.files.dir.entity.FileNode">
        SELECT * FROM files
        WHERE deleted_at IS NOT NULL
          AND owner_id = #{userId}
        ORDER BY deleted_at DESC
    </select>

    <!-- 回收站全量（R-C05 增量）：同上但不分页，供前端一次拉取重建目录树 -->
    <select id="selectRecycleAll" resultType="com.company.cloud.files.dir.entity.FileNode">
        SELECT * FROM files
        WHERE deleted_at IS NOT NULL
          AND owner_id = #{userId}
        ORDER BY deleted_at DESC
    </select>

    <!-- 级联还原（R-C06 第一步）：自身 + 全部已删子孙清 deleted_at。
         不碰 parent_id / name——顶层节点的落点与重名由调用方单独 UPDATE -->
    <update id="restoreCascade">
        WITH RECURSIVE sub AS (
            SELECT id FROM files
            WHERE id = #{id}
              AND owner_id = #{userId}
              AND deleted_at IS NOT NULL
            UNION ALL
            SELECT f.id FROM files f
            JOIN sub s ON f.parent_id = s.id
            WHERE f.deleted_at IS NOT NULL
        )
        UPDATE files
        SET deleted_at = NULL, updated_at = now()
        WHERE id IN (SELECT id FROM sub)
    </update>

    <!-- 子树文件清单（R-C06 彻底删除前置）：收集子树内全部文件节点的 sha256/size -->
    <select id="selectSubtreeFiles" resultType="com.company.cloud.files.recycle.dto.SubtreeFile">
        WITH RECURSIVE sub AS (
            SELECT id FROM files
            WHERE id = #{id}
              AND owner_id = #{userId}
              AND deleted_at IS NOT NULL
            UNION ALL
            SELECT f.id FROM files f
            JOIN sub s ON f.parent_id = s.id
            WHERE f.deleted_at IS NOT NULL
        )
        SELECT id, sha256, size FROM files
        WHERE id IN (SELECT id FROM sub)
          AND is_dir = false
    </select>

    <!-- 级联物理删除（R-C06）：回收站子树整体 DELETE -->
    <delete id="deleteCascadePhysical">
        WITH RECURSIVE sub AS (
            SELECT id FROM files
            WHERE id = #{id}
              AND owner_id = #{userId}
              AND deleted_at IS NOT NULL
            UNION ALL
            SELECT f.id FROM files f
            JOIN sub s ON f.parent_id = s.id
            WHERE f.deleted_at IS NOT NULL
        )
        DELETE FROM files WHERE id IN (SELECT id FROM sub)
    </delete>

    <!-- 释放配额（R-C06）：users 表 Owner 是 A 组，释放配额是 C 组彻底删除职责，直接 SQL -->
    <update id="releaseQuota">
        UPDATE users
        SET used_bytes = GREATEST(0, used_bytes - #{bytes}), updated_at = now()
        WHERE id = #{userId}
    </update>

    <!-- 递减引用计数（R-C07 本地实现）：同 sha256 的存活文件引用减一，GREATEST 防负 -->
    <update id="decrementRefCount">
        UPDATE files
        SET ref_count = GREATEST(0, ref_count - 1), updated_at = now()
        WHERE sha256 = #{sha256}
          AND is_dir = false
          AND deleted_at IS NULL
    </update>

    <!-- R-C06：回收站超期待清理顶层节点（跨全部用户，系统任务）。
         顶层语义与 selectRecycleTopPage 一致：父在回收站中的节点不单独出现，
         随顶层祖先整棵过期；级联软删同事务打标，整树 deleted_at 相同。 -->
    <select id="selectPurgeCandidates" resultType="com.company.cloud.files.dir.entity.FileNode">
        SELECT * FROM files
        WHERE deleted_at IS NOT NULL
          AND deleted_at &lt; #{cutoff}
          AND (parent_id = 0
               OR parent_id NOT IN (SELECT id FROM files WHERE deleted_at IS NOT NULL))
        ORDER BY deleted_at ASC
        LIMIT #{limit}
    </select>

</mapper>

